/**
 * TinyShop global namespace.
 *
 * Every module attaches to this object so the entire app
 * lives under a single global.
 *
 * @since 1.0.0
 * @global
 */
var TinyShop = window.TinyShop = window.TinyShop || {};
